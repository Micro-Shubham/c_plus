#include <iostream>
#include <string >
using namespace std;
int main()
{
    string str = "Programming";
    cout << str.substr(3, 10); // str.substr will give the output from enter index to upto entered index.
    // object.substr(start,end).
    return 0;
}