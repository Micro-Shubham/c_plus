#include <iostream>
#include <string >
using namespace std;
int main()
{
    string str = "Programming";
    cout << str.substr(3,4); // str.substr will give the output from enter index upto number of char endered.
    // object.substr(start,number). 
    return 0;
}